import { Row } from "@/components/Layouts";
import styled from "@emotion/styled";

export const StarWrapper = styled.div`
  display: inline-block;
  position: relative;
`;

export const RatingWrapper = styled(Row.div)``;
